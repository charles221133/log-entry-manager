import React from 'react';

const Header: React.FC = () => (
  <header style={{ padding: '1rem', background: 'var(--color-primary, #1976d2)', color: 'white', textAlign: 'center' }}>
    <h1>Log Entry App</h1>
  </header>
);

export default Header; 